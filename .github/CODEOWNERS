# Rewritten inside this pull request only. If GitHub reads CODEOWNERS from the
# HEAD branch, platform is requested. If it reads from the BASE branch, the
# base file still governs and security-review is requested for
# services/web/package.json.
* @greenlight-lab-rb/platform
